# PhD asset directory

Files here are referenced by the PhD About-the-Author additions on the lander
and by the mdBook online edition at `t27.ai/docs/`.

- `author-fallback.svg` — golden-ring monogram, used when `author.jpg` is absent
- `author.jpg` — drop the actual portrait here (recommended 800×800, will be cropped to circle)

The Trinity v5 React build artifacts in `assets/` are unrelated and untouched.
